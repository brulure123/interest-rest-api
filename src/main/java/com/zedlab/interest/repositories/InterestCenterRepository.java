package com.zedlab.interest.repositories;

import com.zedlab.interest.entities.InterestCenter;
import org.springframework.data.jpa.repository.JpaRepository;

public interface InterestCenterRepository extends JpaRepository<InterestCenter, Long> {
}
